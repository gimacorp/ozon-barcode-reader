# Проверка готового решения

Дата: 9 сентября 2026. Проверки выполнены в рабочем окружении Python 3.12.14, macOS arm64.

| Проверка | Результат |
| --- | --- |
| `python -m pytest -q` | 77 проверок прошли |
| Все исполняемые ячейки `notebooks/demo_ru.ipynb` | 7 ячеек выполнены последовательно |
| `python scripts/inspect_failure.py --box-id SYN-0023` | Ложное значение `O*000904` воспроизведено |
| `python scripts/build_report.py` | PDF и Markdown собраны из сохранённых результатов |
| Визуальный просмотр PDF | Проверена вёрстка всех 22 страниц; подпись графика уточнена и повторно проверена |
| Согласованность метрик | Доли полных множеств совпадают с `results/predictions.jsonl` |
| Локальные ссылки README | Все цели существуют |

В ноутбуке проверено выполнение Python-кода ячеек; отображение графика в Colab выполняется самой средой. Отчётная выборка хранится в `results/`, короткий прогон ноутбука записывает результаты отдельно в `output/quick/`.

Стендовые измерения камер, оптики, конвейера и реального ПЛК выделены в программу промышленного пилота. Следующий практический шаг: проверка торцевой оптики при минимальном модуле этикетки.

Повторный аудит: [матрица соответствия и исправления](audit.md). Дополнительные результаты: `results/replay.json`, `results/native_resolution.json`.
